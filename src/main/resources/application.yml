spring:
  messages:
    basename: messages
    encoding: UTF-8
  config:
    import: SecuritySubmodule/application-kakao.yml
---
spring:
  config:
    activate:
      on-profile: dev
    import: dev/application-db.yml, dev/application-jwt.yml
---
spring:
  config:
    activate:
      on-profile: prod
    import: SecuritySubmodule/application-db.yml, SecuritySubmodule/application-jwt.yml
---
